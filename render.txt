change package.json
// "test": "echo \"Error: no test specified\" && exit 1"
"start" : "node index.js"
below keywords
  "engines": {"node": ">=16.0.0"},

git status
git init
git status
git add .
git commit -m "type msg here"